apiVersion: v1
kind: Service

metadata:
  name: coffee-service
  namespace: default

spec:
  type: LoadBalancer

  selector:
    app: coffee-web

  ports:
  - protocol: TCP
    port: 80
    targetPort: 80